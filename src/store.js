import {
  MAIN_PAGE_ID,
} from './constants';

var store = {
  isRenter: true,
  userId: "ghi",
  currentView: MAIN_PAGE_ID,
  renterUserId: "",
  landlordUserId: "",
  alert: {
    level: "",
  },
};

export default store;
